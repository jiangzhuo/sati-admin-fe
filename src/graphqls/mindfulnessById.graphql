query admin($id: String!) {
  getMindfulnessById(id: $id) {
    code
    message
    data {
      background
      name
      description
      scenes
      price
      createTime
      updateTime
      author
      audio
      copy
      status
    }
  }
}
