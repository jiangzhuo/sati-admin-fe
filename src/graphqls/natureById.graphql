query admin($id: String!) {
  getNatureById(id: $id) {
    code
    message
    data {
      background
      name
      description
      scenes
      price
      createTime
      updateTime
      author
      audio
      copy
      natureAlbums
      status
      validTime
    }
  }
}
