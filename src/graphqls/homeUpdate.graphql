mutation admin($id: String!, $updateData: updateHomeRequest!) {
  updateHome(id: $id, data: $updateData) {
    code
    message
    data {
      id
      position
      type
      resourceId
      background
      name
      description
      createTime
      updateTime
      author
    }
  }
}
