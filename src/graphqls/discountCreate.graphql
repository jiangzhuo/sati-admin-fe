mutation admin($createData: createDiscountRequest!) {
  createDiscount(data: $createData) {
    code
    message
    data {
      id
      type
      resourceId
      background
      name
      discount
      createTime
      updateTime
      beginTime
      endTime
    }
  }
}
