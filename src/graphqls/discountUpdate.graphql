mutation admin($id: String!, $updateData: updateDiscountRequest!) {
  updateDiscount(id: $id, data: $updateData) {
    code
    message
    data {
      id
      type
      resourceId
      background
      name
      discount
      createTime
      updateTime
      beginTime
      endTime
    }
  }
}
