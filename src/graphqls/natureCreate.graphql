mutation admin($createData: createNatureRequest!) {
  createNature(data: $createData) {
    code
    message
    data {
      background
      name
      description
      scenes
      price
      createTime
      updateTime
      author
      audio
      copy
      status
    }
  }
}
